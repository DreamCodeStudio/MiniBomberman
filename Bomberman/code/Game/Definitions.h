#pragma once

//Define the different GAME_STATES
enum GAME_STATE {
	RUN,
	MENU,
};

//Define different game tile states
enum GAME_TILE_STATE {
	BLOCKED,
	EMPTY
};

//Define items
enum GAME_ITEM{
	BOMB,
};
